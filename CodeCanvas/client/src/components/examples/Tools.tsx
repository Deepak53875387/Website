import { Tools } from '../Tools';
import { ThemeProvider } from '../ThemeProvider';

export default function ToolsExample() {
  return (
    <ThemeProvider>
      <Tools />
    </ThemeProvider>
  );
}
