import { TechnicalExpertise } from '../TechnicalExpertise';
import { ThemeProvider } from '../ThemeProvider';

export default function TechnicalExpertiseExample() {
  return (
    <ThemeProvider>
      <TechnicalExpertise />
    </ThemeProvider>
  );
}
